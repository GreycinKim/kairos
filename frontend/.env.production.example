# Build-time vars for production (copy to .env.production or set in your host CI).
# Defaults in code are username `admin` and password `lifeisintheblood` if you omit these.
# VITE_LOGIN_USERNAME=admin
# VITE_LOGIN_PASSWORD=lifeisintheblood

VITE_API_BASE_URL=

# Local dev only — never set in production if you want the login screen.
# VITE_AUTH_DISABLED=true
